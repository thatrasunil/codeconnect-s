{
    "name": "codeconnect-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "vercel-build": "npm --prefix frontend install && npm --prefix frontend run build && mkdir -p public && cp -r frontend/build/* public/"
    }
}